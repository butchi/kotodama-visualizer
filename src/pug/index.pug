extends /_layout/_default-layout

append config-block
  - pageId = 'index'

block content-block
  header
    h1
      | Index Page

  main
    audio#audio(autoplay)
    audio.output(autoplay)
    canvas#canvas
